# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SqliteDataList' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod "SQLite.swift"
  pod "Alamofire"
  

  # Pods for SqliteDataList

  target 'SqliteDataListTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SqliteDataListUITests' do
    # Pods for testing
  end

end
